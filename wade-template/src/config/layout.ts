/**
 * 📏 LAYOUT CONFIGURATION
 * Central source of truth for app dimensions.
 */

export const LAYOUT = {
  // The fixed height of the top navigation bar
  HEADER_HEIGHT: '70px',
  
  // The max width of the center content (prevents stretching on huge screens)
  CONTENT_MAX_WIDTH: '1200px',
};

/**
 * TEMPLATE CONFIGURATION
 */

export const CONFIG ={
  TOP_PAGE: '/login', //This is the default page user is directed to if not logged in
  DASH_PAGE: '/' //This is the page you will navigate to when you log in
}